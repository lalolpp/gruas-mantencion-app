rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /equipos/{doc} {
      allow read, write: if request.auth != null;
    }
    match /registros/{doc} {
      allow read, write: if request.auth != null;
    }
  }
}
